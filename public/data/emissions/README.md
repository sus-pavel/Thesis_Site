# Выбросы

`scope1_results.csv`:

| Поле | Тип | Описание |
|---|---|---|
| `scenario` | string | `base`, `dsm`, `bess` |
| `scope1_tco2_per_year` | number | прямые выбросы, тCO₂/год |

Для реальной выгрузки добавьте `fuel_type`, `fuel_consumption_l`, `emission_factor_kgco2_per_l`, `methodology`, `boundary`, `is_placeholder`.
