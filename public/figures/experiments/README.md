# Эксперименты

Рисунки `exp1_*`, `exp2_*`, `exp3_*` показывают проверки реконструкции, стабильности DR PI и дрейфа кластеров.

Для воспроизводимости рядом в `public/data/experiments` должны быть таблицы с `experiment_id`, вариантом, метрикой, seed и размером выборки.
