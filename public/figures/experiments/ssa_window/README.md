# Тест окна SSA

Файлы `ssa_window_*` сравнивают варианты длины окна по ARI, NMI, корреляции и накопленному вкладу.

На осях обязательно указывайте длину окна в отсчётах/часах и диапазон метрики. Исходные таблицы храните в `public/data/experiments`.
